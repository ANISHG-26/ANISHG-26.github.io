{
  "name": "anish-george-portfolio-tests",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "python -m unittest discover -s tests -v",
    "test:visual": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
